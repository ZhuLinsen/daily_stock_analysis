# Daily Stock Analysis 开发日志

本文档用于记录项目二次开发过程中的功能基线、架构变化、配置变更、测试结果和后续决策。

## 记录规范

后续每次开发按时间倒序新增记录，建议包含：

- 日期与版本/提交号
- 变更目标
- 新增、修改和移除的功能
- 数据库、API、配置或依赖变化
- 验证方式与结果
- 已知问题和后续工作

---

## 2026-06-29：新增收盘后每日情绪复盘模块

### 目标

在现有项目中新增独立的 `sentiment_review` 模块，用于 A 股交易日收盘后的市场情绪复盘。指标必须由透明、可复现的规则计算，LLM 仅负责“行情分析、次日观察方向、风险提示”三类文字总结，不参与或修改任何数值指标。

功能同时支持 Web 手动启动和现有定时任务自动执行；同一市场、同一交易日只保存一条复盘记录，并支持按日期查看及 10/30/60 个交易日历史趋势。

### 实现内容

#### 1. 规则计算与复盘指标

新增纯规则计算器和标准化数据结构，当前复盘内容包括：

- 上涨、下跌、平盘家数及涨跌家数差。
- 全市场成交额及相对上一交易日的变化。
- 涨停家数、炸板家数、炸板率、最高板和连板梯队。
- 2 板、3 板、4 板及以上晋级率。
- 昨日涨停股票今日集合竞价收益和收盘收益的中位数、样本标准差、红盘率。
- 按涨停个股所属行业形成的题材强度排序。
- 基于历史经验分位和市场宽度的规则化情绪状态。
- 数据完整度、数据质量和数据来源追踪信息。

主要计算口径：

- 涨跌家数差 = 上涨家数 - 下跌家数。
- 成交额变化 = 当日成交额 / 上一交易日成交额 - 1。
- 炸板率 = 炸板家数 /（收盘涨停家数 + 炸板家数）。
- 2 板晋级率 = 今日 2 板数 / 昨日 1 板数。
- 3 板晋级率 = 今日 3 板数 / 昨日 2 板数。
- 4 板及以上晋级率 = 今日 4 板及以上数 / 昨日 3 板及以上数。
- 昨日涨停竞价收益 = 今日开盘价 / 昨日收盘价 - 1。
- 历史经验分位 = 历史窗口内小于等于当日值的有效样本数 / 有效样本总数；少于 20 个样本时不输出正式分位。
- 分母为 0 或输入缺失时保存为 `null`，历史曲线不使用 0 补值。

#### 2. 数据源

- AkShare 为主数据源，新增市场宽度、炸板池、昨日涨停今日表现等适配能力。
- 已配置的 Tushare Token 作为可选增强；当 AkShare 当日市场宽度失败时回退到 Tushare，历史日期只允许使用具有日期语义的 Tushare 数据，避免把实时快照错误写入历史复盘。
- 涨停、炸板和晋级证据仍以 AkShare 为主。
- Token 只通过现有配置读取，不写入日志、数据库或复盘返回值。

#### 3. 存储与幂等执行

新增数据库表：

- `sentiment_review_daily`：按市场和交易日保存结构化指标、数据质量、LLM 文本、来源追踪、完整度和版本信息。
- `sentiment_review_stocks`：保存参与计算的个股级证据。

数据库使用 `(market, trade_date)` 唯一约束保证一天一条复盘。完整记录不会被 `partial` 或 `imported` 记录覆盖；正式计算可以升级历史导入或不完整记录。

个股证据按股票代码合并，同一股票可以同时具有 `limit_up`、`broken`、`previous_limit` 等多个 `evidence_types`，避免重复代码触发唯一键冲突。

#### 4. API 与自动任务

新增 `/api/v1/sentiment-review` 路由组：

- `POST /run`：启动最近已收盘交易日或指定历史交易日的复盘。
- `GET /dates`：获取已保存复盘日期。
- `GET /trend`：按指标路径读取 10/30/60 日趋势。
- `GET /{trade_date}`：读取指定日期复盘详情。
- `GET /{trade_date}/stocks`：读取指定日期的个股证据。

新增 `SENTIMENT_REVIEW_ENABLED=true` 配置。现有定时分析完成后会对最近已完成的 A 股交易日执行一次复盘；重复调度通过数据库和执行锁返回已有结果。自动复盘失败不会阻断其他定时任务。

LLM 生成失败时规则指标仍正常保存，并记录 `llm_status=failed`；LLM 不能写回指标区。

#### 5. Web 页面

Web 侧边栏新增“情绪复盘”，路由为 `/sentiment-review`，页面支持：

- 点击“启动今日复盘”执行一次完整计算。
- 选择历史日期查看已保存复盘。
- 展示数据质量、情绪状态、核心指标、题材强度和 LLM 文字总结。
- 点击涨跌家数差、最高板、炸板率、连板晋级率、昨日涨停收盘表现和竞价表现等指标卡打开历史趋势抽屉。
- 趋势抽屉支持 10、30、60 日切换。
- 连板晋级率同时展示“2 板晋级、3 板晋级、4 板及以上”三条曲线。
- 缺失历史点保持断点，不按 0 展示。

#### 6. 历史数据导入

新增 `scripts/import_sentiment_review_history.py`，可将人工核验后的雪球文章或其他公开来源数据以 JSON 形式导入，数据质量标记为 `imported`。生产任务不直接依赖雪球登录态和页面结构，避免反爬或口径变化导致每日任务失败。

使用方式：

```powershell
conda run -n daily_stock python scripts/import_sentiment_review_history.py history.json
```

详细使用说明和公式见 `docs/sentiment-review.md`。

### 配置、API 与数据库变更

| 类型 | 变更 |
|---|---|
| 配置 | 新增 `SENTIMENT_REVIEW_ENABLED`，默认开启 |
| API | 新增 5 个 `/api/v1/sentiment-review` 接口 |
| 数据库 | 新增 `sentiment_review_daily`、`sentiment_review_stocks` |
| Web | 新增 `/sentiment-review` 页面和侧边栏入口 |
| 数据源 | AkShare 主源，Tushare 市场宽度可选增强 |
| 文档 | 新增 `docs/sentiment-review.md` |

### 测试与验证结果

- 使用 Conda `daily_stock`、Python 3.10.20 完成开发和验证。
- 情绪复盘专项后端测试：18 项全部通过。
- 新增测试覆盖规则公式、空值处理、经验分位、仓储幂等、完整记录保护、数据源适配、聚合服务、个股证据合并、执行器和 API。
- Python 新增模块 `compileall` 通过。
- OpenAPI 确认注册 5 个情绪复盘接口。
- Web TypeScript/Vite 生产构建通过，生成独立 `SentimentReviewPage` 资源。
- ESLint 退出码为 0；保留 `SettingsPage.tsx` 中 1 个既有 Hook 依赖警告。
- App 前端测试 7 项全部通过。
- 真实行情烟测成功写入临时数据库：有效收盘交易日为 `2026-06-26`，数据质量为 `complete`，最高板为 6，个股多角色证据合并后未再出现唯一键冲突。
- 烟测未调用 LLM，避免为验证消耗模型额度；LLM 调用路径已通过注入式执行器测试验证。

### 已知问题

- 全库既有测试仍存在 Windows SQLite 临时文件锁、同秒消息排序等波动，与本模块无关。
- 前端全量测试中的告警表单用例要求日股/韩股选项，但现有告警表单只提供 A 股、港股和美股；本次未修改无关告警模块。
- AkShare 全市场快照获取耗时约一分钟，Web 启动复盘接口已单独设置 180 秒超时。
- 新安装或更新代码后需要重启后端进程，并使用最新 Vite 构建产物，才能加载新增 API 和侧边栏页面。

### 版本控制说明

本次工作目录所属父仓库尚无有效 `HEAD`，且项目文件整体处于未跟踪状态，因此未创建提交或分支；所有变更直接保存在当前工作目录。

---

## 2026-06-28：现有功能基线与本地部署

### 基线信息

| 项目 | 当前状态 |
|---|---|
| 上游仓库 | `ZhuLinsen/daily_stock_analysis` |
| 上游分支 | `main` |
| 基线提交 | `dbfd8c1fb846b4cd50461a2f78ae90c0b2a3ec63` |
| 本地目录 | `E:\vibe_coding\AICoding_demo\daily_stock_analysis` |
| Python 环境 | Conda `daily_stock`，Python 3.10.20 |
| Web 前端 | React 19、TypeScript、Vite、Tailwind CSS |
| Web 后端 | FastAPI、Uvicorn |
| 本地入口 | `http://127.0.0.1:8000` |
| 本地数据库 | SQLite：`data/stock_analysis.db` |
| 当前 LLM | DeepSeek `deepseek-v4-flash` |
| 当前搜索源 | Tavily，公共 SearXNG 作为补充/降级源 |

### 一、项目定位

Daily Stock Analysis 是一个基于行情数据、技术指标、新闻搜索和大语言模型的股票研究辅助系统。系统以自选股为入口，聚合市场数据并生成结构化决策报告，同时提供 Web 工作台、命令行、REST API、定时任务、回测、持仓管理和消息推送能力。

项目仅用于研究和辅助分析，不连接券商自动下单，也不应将 AI 输出直接视为投资建议。

### 二、市场与标的支持

- A 股：沪深京股票、指数和 ETF，是当前主要适配市场。
- 港股、美股：支持行情、K 线、新闻和基本分析。
- 日股、韩股：通过 YFinance 支持基础行情和日线分析，部分资金流、龙虎榜和板块能力会降级。
- 股票代码支持命令行覆盖、自选股配置、名称/拼音搜索以及 CSV、Excel、图片和剪贴板导入。
- 内置交易日历和市场阶段判断，可识别盘前、盘中、盘后、非交易日以及未完成日 K。

### 三、股票分析主流程

一次完整个股分析包含以下步骤：

1. 从自选股或手动输入中解析股票代码。
2. 按优先级获取历史日线，支持缓存和断点续传。
3. 获取实时价格、涨跌幅、量比、换手率等行情。
4. 获取筹码分布、所属行业/概念板块和相关市场上下文。
5. 计算均线、趋势、量价、乖离率和风险指标。
6. 搜索最新新闻、风险事件、公司公告、机构观点和业绩预期。
7. 将行情、技术面、新闻、基本面和市场阶段组成分析上下文包。
8. 调用 LLM 生成结构化决策仪表盘。
9. 对 LLM 输出执行市场阶段和交易纪律保护规则。
10. 保存行情、情报、分析历史、上下文快照和 Markdown 报告。

决策报告当前包含：

- 一句话核心结论
- 情绪评分、趋势判断和操作倾向
- 关键价位、止损/止盈和观察条件
- 风险警报与潜在催化因素
- 技术面、筹码和新闻摘要
- 置信度、数据质量和分析时间
- 可执行检查清单

### 四、行情与数据源

项目采用多数据源自动降级机制，当前代码包含：

- Efinance / 东方财富
- 腾讯财经
- AKShare
- Tushare
- Pytdx / 通达信
- BaoStock
- YFinance
- TickFlow
- Longbridge
- Alpha Vantage、Finnhub 等扩展适配器

本机已验证的链路：

- Efinance 成功获取 `600519` 的 39 条日线数据。
- 腾讯财经成功获取实时行情。
- AKShare 成功获取筹码和概念板块数据。
- 尚未配置 Tushare Token 和 TickFlow API Key，当前由免费数据源承担主链路。

### 五、AI 与搜索能力

#### LLM

项目使用 LiteLLM 统一模型调用层，支持：

- DeepSeek
- Gemini
- OpenAI 及 OpenAI 兼容接口
- Anthropic Claude
- 通义千问、智谱、Kimi、MiniMax、火山引擎、硅基流动、OpenRouter 等渠道
- Ollama 本地模型
- 多渠道、主模型、Agent 模型和 fallback 模型配置

本机当前已启用 DeepSeek `deepseek-v4-flash`，真实鉴权和完整报告生成均已通过。

#### 新闻与情报搜索

项目支持：

- Tavily
- SerpAPI
- Anspire Search
- 博查、Brave、MiniMax Search
- 自建或公共 SearXNG

搜索结果会执行时间窗口过滤、股票相关度排序、低质量内容过滤和分类整理。本机当前 Tavily 已验证可用；公共 SearXNG 偶有 `429 Too Many Requests`，失败时不会阻断主分析流程。

### 六、Agent 与策略问股

Web `/chat` 页面提供多轮策略问股。Agent 可以调用行情、K 线、技术指标、新闻和风险信息，并根据策略生成解释性结论。

当前内置 15 套策略：

1. 多头趋势
2. 均线金叉
3. 缩量回踩
4. 放量突破
5. 箱体震荡
6. 底部放量
7. 一阳三阴
8. 龙头策略
9. 情绪周期
10. 热点题材
11. 事件驱动
12. 成长质量
13. 预期重估
14. 缠论
15. 波浪理论

策略以 YAML 定义，可作为后续新增策略、调整参数和开发多 Agent 编排的扩展点。

### 七、Web 可视化工作台

当前前端提供以下主要页面：

| 页面 | 主要功能 |
|---|---|
| 首页 | 自选股、手动分析、任务进度、分析结果和历史报告 |
| Agent 问股 | 多轮问答、策略选择、工具调用和会话管理 |
| 股票筛选 | AlphaSift 多因子候选筛选和扫描结果 |
| 回测 | 对历史分析结果进行收益和有效性评估 |
| 持仓 | 账户、持仓、交易计划和风险信息管理 |
| 决策信号 | 查看结构化信号、后验结果和有效性反馈 |
| 告警 | 创建规则、执行 dry-run、查看触发历史和通知状态 |
| Token 用量 | 查看 LLM 调用次数、Token 和成本统计 |
| 系统设置 | 管理模型、数据源、股票列表、通知和运行参数 |
| 登录 | 可选管理员认证和登录限流 |

本机 WebUI 当前仅监听 `127.0.0.1:8000`，未向局域网或公网开放，管理员认证暂未启用。

### 八、API 能力

FastAPI 提供 `/api/v1` 版本化接口，当前路由分组包括：

- `auth`：管理员认证
- `agent`：策略问股和 Agent 会话
- `analysis`：个股分析和大盘复盘任务
- `history`：历史报告和分析记录
- `stocks`：股票搜索、自选股和导入解析
- `backtest`：回测任务与结果
- `system`：配置读取、保存和首次设置状态
- `usage`：模型调用和 Token 用量
- `portfolio`：持仓与账户风险
- `alerts`：告警规则、执行记录和冷却状态
- `decision-signals`：决策信号和后验评价
- `alphasift`：多因子选股
- `intelligence`：新闻和市场情报
- `health`：服务健康检查

健康接口 `/health` 和 `/api/v1/health` 已在本机验证返回 HTTP 200。

### 九、回测、持仓与信号评估

- 支持对已保存的历史分析结果执行回测。
- 支持设置评估窗口、强制重算和单股票回测。
- 保存结构化决策信号，并在后续交易日计算实际结果。
- 支持对信号提交“有用/无用”反馈。
- 持仓模块提供成本、数量、盈亏、风险和交易计划信息。
- 自动回测会在常规分析完成后检查满足时间条件的历史信号。

### 十、大盘复盘

大盘复盘功能可以：

- 汇总主要指数和市场涨跌家数。
- 统计成交额、涨停、跌停和市场宽度。
- 生成行业、概念板块涨跌排名。
- 搜索市场新闻和热点事件。
- 使用 LLM 生成复盘报告；没有 LLM 时降级为规则模板。
- 将复盘保存为 Markdown 和数据库历史记录。

### 十一、通知与自动化

项目内置的通知发送器包括：

- 企业微信、飞书
- Telegram、Discord、Slack
- 邮件
- Server 酱、PushPlus
- ntfy、Gotify、Pushover
- 自定义 Webhook 和 AstrBot

支持汇总推送和单股完成后立即推送。本机目前未配置通知渠道，因此报告只保存在本地。

运行方式包括：

- 单次命令行分析
- dry-run 数据验证
- 本地定时任务
- FastAPI / WebUI 长运行服务
- GitHub Actions
- Docker / Docker Compose
- 桌面端打包

### 十二、存储、配置与安全

- 使用 SQLite 保存行情、新闻情报、分析历史、任务、回测、持仓、告警和 LLM 用量。
- 默认使用 WAL、busy timeout 和写入重试降低并发锁冲突。
- `.env` 保存 API Key 和运行参数，并已被 Git 忽略。
- 支持上下文快照，便于复现某次分析的输入数据。
- WebUI 可以启用管理员认证；公网部署时必须开启认证并配置可信代理边界。
- 当前本机服务只绑定回环地址，因此其他设备无法直接访问。

### 十三、CLI 入口

常用命令：

```powershell
# 获取数据但不调用 AI
conda run --no-capture-output -n daily_stock python main.py --dry-run --stocks 600519 --no-notify --force-run

# 完整个股分析
conda run --no-capture-output -n daily_stock python main.py --stocks 600519 --no-notify --force-run

# 仅运行大盘复盘
conda run --no-capture-output -n daily_stock python main.py --market-review

# 启动 WebUI
conda run --no-capture-output -n daily_stock python main.py --webui-only --host 127.0.0.1 --port 8000

# 启动本地定时任务
conda run --no-capture-output -n daily_stock python main.py --schedule --no-run-immediately
```

### 十四、本次本地验证结果

- Python 依赖安装完成，`pip check` 无冲突。
- DeepSeek 最小生成请求成功。
- Tavily 单次搜索成功。
- `600519` 完整分析成功，LLM 返回结构化结果并保存 Markdown 报告。
- Web 首页、健康接口和首次配置状态接口均返回 HTTP 200。
- 前端构建成功，生成 43 个静态文件。
- 本地数据库已创建并保存行情、情报和分析数据。

### 十五、已知问题与二次开发注意事项

1. 免费行情和公共搜索接口可能限流、超时或改变字段，需要保留降级链路和结构化错误信息。
2. 当前未配置 Tushare、TickFlow 和通知渠道；这些不是基础分析的硬依赖。
3. WebUI 当前未启用管理员认证，只能保持 `127.0.0.1` 本机访问；开放局域网或公网前必须补认证与安全配置。
4. npm 审计当前报告 5 项生产依赖通告，集中于 Axios、React Router 及其传递依赖。后续升级需在独立分支完成并执行前端完整回归，不能直接使用强制修复。
5. `conda run` 在旧版 Conda 22.9 下捕获中文/Unicode 输出可能触发 GBK 编码问题，Windows 上统一使用 `--no-capture-output` 和 UTF-8 终端。
6. AI 分析结果依赖数据完整性、Prompt、模型版本和新闻质量；必须保存来源、时间戳和数据质量状态。
7. 回测和二次策略开发需要严格处理未来函数、复权、停牌、涨跌停、T+1、手续费和滑点。
8. 后续修改数据库结构、API 响应或 `.env` 配置时，应同步记录迁移与兼容策略。

### 十六、建议的二次开发方向

- 增加自定义选股因子和策略参数可视化编辑器。
- 建立策略版本、样本外回测和参数实验管理。
- 强化 A 股公告、财务报表和行业数据的可靠来源。
- 增加报告证据引用和数据溯源页面。
- 完善告警规则、持仓联动和复盘闭环。
- 为关键数据源、LLM 和搜索渠道增加可观测性与成本预算。
- 升级前端依赖并建立安全审计与自动回归流程。

---

## 后续日志模板

```markdown
## YYYY-MM-DD：变更标题

### 目标

### 实现内容

### 配置/API/数据库变化

### 测试结果

### 已知问题

### 后续工作
```
